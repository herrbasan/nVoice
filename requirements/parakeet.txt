# Parakeet-TDT 0.6B v3 — HuggingFace Transformers (not NeMo — NeMo crashes on Windows)
# Requires its own venv with transformers + torch+CUDA.
# Python 3.10 or 3.11 recommended (transformers wheel compatibility).
#
# IMPORTANT: PyTorch must be installed with CUDA support. The default PyPI
# wheel is CPU-only. Install torch FIRST from the CUDA index:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Then install the rest of this file with: pip install -r requirements/parakeet.txt

# PyTorch with CUDA (install first via --index-url, see above)
torch
torchvision
torchaudio

# HuggingFace Transformers — pipeline-based ASR
transformers
accelerate

# Audio feature extraction (required by ParakeetFeatureExtractor)
librosa

# Shared worker runtime
fastapi
uvicorn
websockets
soundfile
numpy
onnxruntime  # shared Silero VAD backend (vad.py) — without it the realtime
             # strategy falls back to RMS-only gating and transcribes noise
requests
